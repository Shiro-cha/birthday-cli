export type Wisdom = {
    quote: string;
    advice: string;
    funFact: string;
  };
  
  export type ApiConfig = {
    quotesApiUrl: string;
    adviceApiUrl: string;
    catFactApiUrl: string;
    quotesApiKey: string;
  };